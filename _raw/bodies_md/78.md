\- 특정 서비스가 정상저긍로 동작하지 않을경우 다른 기능으로 대체 수행 

\- 장애가 발생하는 서비스에 반복적인 호출이 되지 못하게 차단. 

nexflix-Hystrix 는 더이상 지원하지 않을 예정임으로 ->**Resilience4j 사용.**

\- resilience4j-circuitbreaker
    
    
    <dependency>
       <groupId>org.springframework.cloud</groupId>
       <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
    </dependency>

주문 목록을 확인하는 api 인 order-service/{userId}/orders 에 대해서 
    
    
    @FeignClient(name="order-service",configuration = FeignErrorDecoder.class)
    public interface OrderServiceClient {
    
        @GetMapping("/order-service/{userId}/orders")
        List<ResponseOrder> getOrders(@PathVariable String userId);
    }
    
    List<ResponseOrder> orderList = orderServiceClient.getOrders(userId);

breaker 를 적용하여 호출한다.

에러가 발생했을경우에 빈[] 로 대체해서 보여준다. 
    
    
    CircuitBreaker circuitBreaker
            = circuitBreakerFactory.create("circuitbreaker");
    List<ResponseOrder>  orderList = circuitBreaker.run(
            ()->
        orderServiceClient.getOrders(userId),
        throwable ->new ArrayList<>());

Custom 한 circuitbreaker 설정
    
    
    @Bean
    public Customizer<Resilience4JCircuitBreakerFactory> globalCustomConfiguration() {
    
        CircuitBreakerConfig circuitBreakerConfig = CircuitBreakerConfig.custom()
                .failureRateThreshold(4) // 4번 실패하면 실행.
                .waitDurationInOpenState(Duration.ofMillis(1000)) // 실행하고 유지 지속 시간. 1초
                .slidingWindowType(CircuitBreakerConfig.SlidingWindowType.COUNT_BASED) // 
                .slidingWindowSize(2)
                .build();
                
                
            TimeLimiterConfig timeLimiterConfig
                    = TimeLimiterConfig.custom()
                    .timeoutDuration(Duration.ofSeconds(4))
                    .build(); // timeout 기준 4초 
                    
              return resilience4JCircuitBreakerFactory -> resilience4JCircuitBreakerFactory.configureDefault(id->new Resilience4JConfigBuilder(id)
                    .timeLimiterConfig(timeLimiterConfig)
                    .circuitBreakerConfig(circuitBreakerConfig)
                    .build());
           
    }

user서비스에서 order service를 사용할때 order-service에 문제가 생겼을 경우 대응하기 위한 것 이러한 유형을 circuitBreaker 라고 한다. 

user 서비스를 사용하다가 order 서비스가 죽었다고 해서 user 서비스의 다른 곳들도 죽어 서비스를 사용하지 못하면 

안되기 때문에 적절하게 circuitbreaker를 적용해서 임시적으로 order 서비스에 대한 호출을 막고, user서비스를 안정화 시키는

장치이다.
