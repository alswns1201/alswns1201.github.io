안녕하세요 오늘은 호출 흐름에 대한 이해를 바탕으로 Redisson을 통해 Rock을 제어해 보겠습니다. 저번시간에는 Redis를 통해서 cache된 데이터를 통해 데이터를 호출하는 것을 활용해 보았다면, 이번에는 `데이터 제어`를 다뤄 보겠습니다.  
  


\- 사전 지식 알아보기 -

더보기

**[1] 동기 방식 : Synchronous**

\- 하나의 작업이 완료될 때까지 다음 작업을 시작하지 않습니다  
**[2] 비동기 (Asynchronous)**  
\- "일단 시켜놓고 다른 일 함" 방식

**[3] 동시성 (Concurrency) - "여러 일을 번갈아 가며" 방식**

비유: 당신은 한 번에 여러 일을 매우 빠르게 번갈아 가며 처리합니다. 예를 들어 라면 면 넣고 -> 볶음밥 재료 볶고 -> 설거지 접시 닦고 -> 다시 라면 확인하고...

[4] **병렬성 (Parallelism) - "여러 일을 동시에" 방식**

비유: A는 thread-1을, B는thread-2을, C는 thread-3를 각자 독립적으로 동시에 진행합니다.

  
일단 일을 시켜놓고 다른일을 진행하는 일에는 비동기 처리가 적합합니다. 즉 일단 시켜놓고 다른 일을 하는" **비동기 처리** 에 매우 적합합니다. 예시로 이메일을 보내는 예시로 구현해보려 합니다.

\- 이메일 비동기처리 필요성 -

더보기

1\. **사용자 경험 저하:**

이메일 전송에 2초가 걸린다면, 사용자는 회원가입 버튼을 누른 후 2초 동안 웹 페이지가 멈춘 것처럼 느껴질 수 있습니다.2\. **서버 자원 낭비:**

이메일 전송을 기다리는 2초 동안 해당 웹 서버 스레드는 아무것도 하지 못하고 블로킹됩니다. 이는 다른 사용자의 요청을 처리하는 데 사용될 수 있는 귀중한 서버 자원이 낭비되는 것을 의미합니다. 동시 사용자 수가 많아지면 서버 전체의 응답 속도가 현저히 느려지거나 먹통이 될 수 있습니다.

\- 비동기 Springboot에서 동작하기.- 

위와 같은 비동기 처리의 필요성이 있습니다. Springboot에서 활용해 볼 예정이며, 비동기 @Async 를 사용하기 위해서는 

비동기를 쓰겠다는 enable설정을 해야합니다. @EnableAsync를 적용했다면 @Async를 적용한 메소드는 비동기로 동작합니다.
    
    
    @SpringBootApplication
    @EnableAsync
    public class RedisApplication {
    
       public static void main(String[] args) {
          SpringApplication.run(RedisApplication.class, args);
       }
    
    }

sednEmail은 이제 비동기로 동작합니다. redissonClient 에 대해서는 밑에서 계속 다뤄 보겠습니다. 

@Async와 관련된 내용은 이전 글에서 다뤘던 글을 확인해주시면 감사하겠습니다 ^^( Thread Pool 의 개념)
    
    
    @Async("EmailExecutor") // 이메일 전송 작업을 별도의 스레드에서 실행
    public CompletableFuture<String> sendEmail(String email) { // 반환 타입을 String으로 변경하여 결과 메시지를 전달
        String lockKey = "lock:email:" + email;
        RLock lock = redissonClient.getLock(lockKey); // 이메일 주소별로 락 생성
    
        String resultMessage;
        try {
            // 락을 획득하되, 기다리지 않고 즉시 시도 (0초 대기)
            // 락 획득에 성공하면 10초 후에 자동으로 락이 해제되도록 설정
            if (lock.tryLock(0, 10, TimeUnit.SECONDS)) {
                try {
                    System.out.println(Thread.currentThread().getName() + " - 이메일 전송 시작: " + email);
                    Thread.sleep(2000); // 이메일 전송 시뮬레이션 (2초 소요)
                    System.out.println(Thread.currentThread().getName() + " - 이메일 전송 완료: " + email);
                    resultMessage = "이메일 전송 성공: " + email;
                } finally {
                    // 락이 현재 스레드에 의해 보유 중인 경우에만 해제
                    // (autoUnlock 기능이 있어도 명시적으로 해제하는 것이 안전)
                    if (lock.isHeldByCurrentThread()) {
                        lock.unlock();
                    }
                }
            } else {
                // 락 획득 실패 (다른 스레드/서버에서 이미 처리 중)
                System.out.println(Thread.currentThread().getName() + " - 이미 락이 걸려 있어 이메일 전송 스킵: " + email);
                resultMessage = "이메일 전송 스킵 (이미 처리 중): " + email;
            }
        } catch (Exception e) {
            e.printStackTrace();
            resultMessage = "이메일 전송 중 오류 발생: " + e.getMessage();
        }
    
        return CompletableFuture.completedFuture(resultMessage);
    }

이제 redis 의 redisson 을 이용하여 rock 을 관리 해 보겠습니다. lock.getLock을 통해서 현재 시도하려는 key를 만들고 해당 key로 lock을 try 해봤을때 열지 못하는 key라면 락이 걸려있다고 판단해보겠습니다. 

  * **lock.tryLock(...) :** 이 열쇠로 금고 문을 지금 당장 열 수 있는지 시도하는 것입니다. 열리면 락 획득 성공, 안 열리면 실패.
  * **lock.lock() :** 이 열쇠로 금고 문을 열릴 때까지 기다리는 것입니다. 문이 열릴 때까지 여기서 대기합니다.



![](https://blog.kakaocdn.net/dna/bhGVAT/btsQ2qnv0Wx/AAAAAAAAAAAAAAAAAAAAAK6KGo0ZNeBJPSiJJsu8PG-Cab22d08zrJpvV62eb2RQ/img.png?credential=yqXZFxpELC7KVnFOS48ylbz2pIh7yKj8&expires=1788188399&allow_ip=&allow_referer=&signature=Pym0Znq2AXNlBqREsUrvVbgpvM8%3D)

redisson의 핵심은 분산 입니다.

서로 다른 JVM에서 호출하더라도 **모두 Redis에 있는 동일한 lock:email:user1@example.com이라는 이름의 락을 가리키게 됩니다.   
  
실무에서는 보통 세션 관리 를 redis로 많이 한다고 합니다. 다음에는 기회가 된다면 redis를 통한 sesison 관리도 다뤄 보겠습니다. 감사합니다.**
